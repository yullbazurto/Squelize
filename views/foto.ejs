<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title><%= foto.titulo %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-5">
  <h1><%= foto.titulo %></h1>
  <p><strong>Descripción:</strong> <%= foto.descripcion %></p>
  <p><strong>Calificación:</strong> <%= foto.calificacion %></p>
  <p><strong>Fecha:</strong> <%= foto.createdAt.toLocaleString() %></p>
  <img src="/images/<%= foto.ruta %>" alt="<%= foto.titulo %>" class="img-thumbnail" style="max-width: 300px;">
  
  <h4 class="mt-4">Etiquetas</h4>
  <% if (foto.etiqueta && foto.etiqueta.length > 0) { %>
    <% foto.etiqueta.forEach(et => { %>
      <span class="badge bg-info text-dark"><%= et.texto %></span>
    <% }) %>
  <% } else { %>
    <p class="text-muted">Sin etiquetas</p>
  <% } %>

  <div class="mt-4">
    <a href="/fotos/findAll/view" class="btn btn-secondary">Volver</a>
  </div>
</body>
</html>
